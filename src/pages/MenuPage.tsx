// MenuPage was removed and saved as oldmenupage.txt in the same folder as a draft.
// This file intentionally left minimal to avoid build errors if imported elsewhere.
const MenuPage = () => null;
+
+export default MenuPage;
